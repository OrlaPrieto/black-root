---
import { Image } from 'astro:assets';
import brandedBags from '../assets/branded_bags.png';
---

<section id="inicio" class="hero">
  <div class="hero-overlay"></div>
  <div class="container hero-content">
    <div class="hero-text reveal">
      <h1>Fuerza que nace <span class="accent">del origen</span></h1>
      <p>Carbón 100% vegetal para los paladares más exigentes. Calidad artesanal en cada bolsa.</p>
      <div class="hero-actions">
        <a href="#productos" class="btn-primary">Ver Productos</a>
        <a href="#nosotros" class="btn-outline">Conoce Más</a>
      </div>
    </div>
    
    <div class="hero-image reveal">
      <div class="floating-image">
        <Image src={brandedBags} alt="Black Root Charcoal Bags" width={500} />
      </div>
    </div>
  </div>
</section>

<style>
  .hero {
    position: relative;
    min-height: 100vh;
    display: flex;
    align-items: center;
    overflow: hidden;
    padding-top: 100px;
    background: radial-gradient(circle at 20% 50%, #1a1a1a 0%, #0a0a0a 100%);
  }
  
  .hero-overlay {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: url('https://www.transparenttextures.com/patterns/dark-matter.png');
    opacity: 0.3;
  }
  
  .hero-content {
    display: grid;
    grid-template-columns: 1.2fr 1fr;
    gap: 4rem;
    align-items: center;
    position: relative;
    z-index: 2;
  }
  
  h1 {
    font-size: 5rem;
    line-height: 0.9;
    margin-bottom: 2rem;
  }
  
  .accent {
    color: var(--accent-red);
    text-shadow: 0 0 20px rgba(211, 47, 47, 0.4);
  }
  
  p {
    font-size: 1.2rem;
    color: var(--text-muted);
    max-width: 500px;
    margin-bottom: 3rem;
  }
  
  .hero-actions {
    display: flex;
    gap: 1.5rem;
  }
  
  .btn-outline {
    display: inline-block;
    padding: 0.8rem 2.2rem;
    border: 1px solid var(--accent-tan);
    color: var(--accent-tan);
    text-decoration: none;
    font-weight: 600;
    border-radius: 4px;
    text-transform: uppercase;
    transition: var(--transition-smooth);
  }
  
  .btn-outline:hover {
    background: var(--accent-tan);
    color: var(--bg-dark);
  }
  
  .hero-image {
    display: flex;
    justify-content: center;
  }
  
  .floating-image {
    position: relative;
    animation: floating 6s ease-in-out infinite;
  }
  
  .floating-image::after {
    content: '';
    position: absolute;
    bottom: -40px;
    left: 10%;
    width: 80%;
    height: 20px;
    background: rgba(0,0,0,0.5);
    filter: blur(20px);
    border-radius: 50%;
  }

  @keyframes floating {
    0% { transform: translateY(0px); }
    50% { transform: translateY(-20px); }
    100% { transform: translateY(0px); }
  }
  
  @media (max-width: 968px) {
    .hero {
      padding: 120px 0 60px;
      min-height: auto;
    }
    .hero-content {
      grid-template-columns: 1fr;
      text-align: center;
      gap: 2rem;
    }
    
    h1 { font-size: 3.2rem; }
    
    .hero-text {
      display: flex;
      flex-direction: column;
      align-items: center;
      padding: 0 1.5rem;
    }
    
    .hero-actions {
      justify-content: center;
      flex-wrap: wrap;
      gap: 1rem;
    }

    .hero-image {
      margin-top: 2rem;
    }

    .floating-image img {
      max-width: 280px !important;
      height: auto !important;
    }
  }

  @media (max-width: 480px) {
    .hero {
      padding-top: 100px;
    }
    h1 { 
      font-size: 2.6rem; 
      line-height: 1.1;
      max-width: 12ch;
      margin-left: auto;
      margin-right: auto;
    }
    p { 
      font-size: 1rem; 
      margin-bottom: 2rem;
    }
    .hero-actions {
      flex-direction: column;
      width: 100%;
      padding: 0 1rem;
    }
    .btn-primary, .btn-outline {
      width: 100%;
      text-align: center;
      padding: 0.7rem 1.5rem;
    }
    .floating-image img {
      max-width: 220px !important;
    }
  }
</style>
