---
import { Image } from 'astro:assets';
import logo from '../assets/logo.jpg';

const features = [
  { title: "100% Vegetal", description: "Carbón de origen natural, sin aditivos químicos para un sabor puro." },
  { title: "Alta Duración", description: "Selección de maderas que garantizan brasas por más tiempo." },
  { title: "Menor Residuo", description: "Proceso de producción artesanal que resulta en ceniza mínima." },
  { title: "Sabor Superior", description: "Ideal para parrillas, hornos y fogatas, resaltando el sabor de tus cortes." }
];
---

<section id="nosotros" class="about">
  <div class="container grid">
    <div class="about-image reveal">
      <div class="kraft-card">
        <Image src={logo} alt="Black Root Seal" width={300} />
      </div>
    </div>
    
    <div class="about-text reveal">
      <span class="subtitle">Nuestra esencia</span>
      <h2>El secreto está en <span class="accent">las raíces</span></h2>
      <p>Black Root nace del compromiso con la calidad y la tradición. Hecho en México con producción artesanal, nuestro carbón es el resultado de una cuidadosa selección de maderas para ofrecerte el mejor rendimiento en tu asado.</p>
      
      <div class="features-grid">
        {features.map(f => (
          <div class="feature">
            <h3>{f.title}</h3>
            <p>{f.description}</p>
          </div>
        ))}
      </div>
    </div>
  </div>
</section>

<style>
  .about {
    background: #0f0f0f;
  }
  
  .grid {
    display: grid;
    grid-template-columns: 1fr 1.2fr;
    gap: 6rem;
    align-items: center;
  }
  
  .kraft-card {
    background: #c5a07d33;
    padding: 3rem;
    border: 1px solid var(--accent-tan);
    display: inline-block;
    position: relative;
    border-radius: 4px;
  }
  
  .kraft-card::before {
    content: '';
    position: absolute;
    top: -15px;
    right: -15px;
    bottom: 15px;
    left: 15px;
    border: 1px solid var(--accent-tan);
    z-index: -1;
  }
  
  .subtitle {
    color: var(--accent-red);
    text-transform: uppercase;
    font-weight: 700;
    letter-spacing: 0.2em;
    font-size: 0.9rem;
  }
  
  h2 {
    font-size: 3.5rem;
    margin: 1rem 0 2rem;
    line-height: 1.1;
  }
  
  .accent {
    color: var(--accent-tan);
  }
  
  .features-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 2rem;
    margin-top: 3rem;
  }
  
  .feature h3 {
    color: var(--accent-tan);
    font-size: 1.4rem;
    margin-bottom: 0.5rem;
  }
  
  .feature p {
    font-size: 0.9rem;
    color: var(--text-muted);
  }
  
  @media (max-width: 968px) {
    .grid {
      grid-template-columns: 1fr;
      text-align: center;
      gap: 3rem;
    }
    .about-image {
      order: 2;
    }
    .features-grid {
      text-align: left;
    }
    h2 { font-size: 2.8rem; }
  }

  @media (max-width: 480px) {
    .kraft-card {
      padding: 1.5rem;
    }
    .features-grid {
      grid-template-columns: 1fr;
    }
    h2 { font-size: 2.2rem; }
  }
</style>
